#pragma once
const float seaLevel = -1.0f;
const float planeSpeed = 0.16f;
const float enemySpeed = 0.14f;
const float bulletSpeed = 0.7f;
const float planeTurnSpeed = 0.03f;
const int sectorTileRadius = 2;
const float tileSize = 12.0f;
const float sectorFloatRadius = sectorTileRadius * tileSize;
const int visibleTileRadius = 10;
const float isletTouchDistance = 5.0f;
const int visibleTileDiameter = 2 * visibleTileRadius + 1;
const float visibleRadius = visibleTileRadius * tileSize;
const double endAfterSeconds = 1.0;